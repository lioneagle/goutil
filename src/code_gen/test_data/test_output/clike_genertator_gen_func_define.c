/* NAME: parse
 * PARAMS:
 * context -- context for parsing
 * src     -- source to parse
 * len     -- length of source
 * RETURN:
 * length parsed
 * NOTE: create for test
 */
int parse(Context* context, char const* src, int len)
{
    return 0;
}

/*********************************
 * NAME: parse
 * PARAMS:
 * context -- context for parsing
 * src     -- source to parse
 * len     -- length of source
 * RETURN:
 * length parsed
 * NOTE: create for test
 **********************************/
int parse(Context* context,
    char const* src,
    int len) {
    return 0;
}
